#!/usr/bin/perl
#
#   Write a program that takes in multiple directorynames from the command line, then
#   prints out their contents. Use a function that takes a directoryhandle reference that
#   you made using IO::Dir

